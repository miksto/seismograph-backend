version: '3'
services:
  socket-server:
    build:
      context: ./
      dockerfile: Dockerfile-socket-server
    volumes:
       - "/Users/miksto/www:/home/app/files/"
    ports:
      - "3000:3000"
  
  file-server:
    image: nginx
    volumes:
       - "/Users/miksto/www/images:/usr/share/nginx/html"
    ports:
      - "8080:80"

